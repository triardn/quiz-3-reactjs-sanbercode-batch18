import React from 'react';

const MovieListEditor = () => {
    return (
        <div className="container">
            Ini halaman movie list editor
        </div>
    );
};

export default MovieListEditor;